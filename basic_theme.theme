<?php
use Drupal\image\Entity\ImageStyle;

//basic_theme
function basic_theme_preprocess_node(&$variables)
{
    // $node = $variables['node'];
    // $variables['image_url'] = file_create_url($node->field_image->entity->getFileUri());
    // $image_uri = $node->field_image->entity->getFileUri();
    // $url = ImageStyle::load('thumbnail')->buildUrl($image_uri);
    // kint($url);
}



